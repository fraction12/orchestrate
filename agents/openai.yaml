interface:
  display_name: "Orchestrate"
  short_description: "Run ticket sets through agent orchestration"
  default_prompt: "Use $orchestrate to run this ticket through planning, workers, review, PR, UAT, and cleanup."
